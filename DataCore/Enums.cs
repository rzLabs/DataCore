namespace DataCore
{
    public enum SortType
    {
        Name = 0,
        Offset = 1,
        Size = 2,
        DataId = 3
    }

    public enum ExtensionType
    {
        Encrypted = 0,
        Unencrypted = 1
    }
}
